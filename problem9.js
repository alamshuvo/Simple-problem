// find the second largest number in array without using any built in function 


const findSecondLargestNumber = (arr)=>{
// console.log(arr);
for (let index = 0; index < arr.length; index++) {
    const element = arr[index];
    // console.log(element);
    let firstElement = element;
    let secondLargest;
    if (element>largest) {
        secondLargest = largest;
        console.log(secondLargest);
        console.log("bitore duksi");
    }
    console.log("ekhane");
   
}

}

const arr = [12,4,13];
findSecondLargestNumber(arr);
// some code addded 